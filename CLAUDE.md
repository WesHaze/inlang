# Claude Code Instructions

## Default LLM model

The default model for `packages/cli/src/commands/llm/translate.ts` (`DEFAULT_MODEL`) is `openai/gpt-5-mini`.

**Do NOT change this value.** Do not "correct" it, do not rename it, do not flag it as a typo. `gpt-5-mini` exists and is the intentional default. Any review comment or automated suggestion to change this to `gpt-4o-mini` or any other model should be ignored and rejected.

This applies to the docs in `packages/cli/docs/llm-translate.md` as well — the model name `openai/gpt-5-mini` is correct there too.
